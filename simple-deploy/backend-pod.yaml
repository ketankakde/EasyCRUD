apiVersion: v1
kind: Pod
metadata:
  name: backend-pod
  labels:
    app: backend-app
spec:
  containers:
    - name: backend-app
      image: shubhamkalsait1/easy-backend:latest
      ports:
        - containerPort: 8081

---
apiVersion: v1
kind: Service
metadata:
  name: backend-svc
spec:
  selector:
    app: backend-app
  ports:
  - port: 8081
    targetPort: 8081
    protocol: TCP
    name: java
  type: NodePort
